#include<stdio.h>
int main()
{int arr[5];
 int i,max=0;
 for(i=0;i<=4;i++)
 {printf("Enter the element  ");
  scanf("%d",&arr[i]);
 }
 for(i=0;i<=4;i++)
 {
 
 if(arr[i]>max)
  {   max=arr[i];
 }
 }
 printf("The max of the array is %d",max);
return 0;
}
 
