#include<stdio.h>
int main()
{int age;
 printf("Program to check if the person is eligible to vote.\n Enter your age  ");
 scanf("%d",&age);
 if(age>=18)
  {printf("You are eligible to vote.");
  }
 else
  {printf("You are not eligible to vote.");
  }
return 0;
}
